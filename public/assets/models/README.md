# 3D 모델 에셋

| 파일 | 용도 |
|------|------|
| `prop.glb` | 위치 기반 AR 씬(`public/ar-location.html`)에서 GPS 좌표 상공 1.5m에 표시할 오브젝트 |

- `prop.glb` 를 이 폴더에 넣으면 `/ar-location.html` 에서 자동 로드됩니다.
- 없으면 모델만 안 보일 뿐 씬은 동작합니다.
